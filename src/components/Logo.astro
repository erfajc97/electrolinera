---
const { className = "" } = Astro.props;
---
<a href="/" aria-label="Disart Energy — ir al inicio" class={`group inline-flex items-center gap-2 ${className}`}>
  <span class="relative grid h-9 w-9 place-items-center rounded-xl bg-gradient-to-br from-brand-500 to-brand-700 shadow-soft">
    <svg viewBox="0 0 32 32" class="h-5 w-5 text-white" fill="currentColor" aria-hidden>
      <path d="M18 5 L9 18 H15 L13 27 L23 13 H17 Z"/>
    </svg>
    <span class="absolute inset-0 rounded-xl ring-1 ring-inset ring-white/30"></span>
  </span>
  <span class="font-display text-lg font-bold tracking-tight">
    Disart<span class="text-brand-600">Energy</span>
  </span>
</a>
